fastapi==0.115.6
uvicorn[standard]==0.34.0
# Was left unpinned when it only existed here to satisfy
# uvicorn[standard] (nothing imported it directly, and pinning
# conflicted with google-genai's own <15.0 constraint on a fresh
# install). Now pinned: tests/test_live_factory_mqtt_e2e.py (Step 20)
# imports websockets.sync.client directly, and 14.2 already satisfies
# google-genai's <15.0 constraint.
websockets==14.2
requests==2.34.2
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
mcp==1.2.0
groq==0.15.0
google-genai==0.5.0
neo4j==5.27.0
PyYAML==6.0.2
numpy==2.2.1
pytest==8.3.4
httpx==0.28.1
langgraph==1.2.9
pypdf==5.1.0
sentence-transformers==5.6.0
ultralytics==8.4.96
# headless deliberately: this is a server backend that runs YOLO
# inference on video frames and never displays them (confirmed no
# cv2.imshow/namedWindow/waitKey anywhere in app/), and regular
# opencv-python needs libGL.so.1 and other X11/GUI libraries that a
# minimal container image doesn't have, which crashed the container
# on startup (ImportError: libGL.so.1) until this was found and fixed.
opencv-python-headless==5.0.0.93
pandas==2.2.3
playwright==1.61.0
python-multipart==0.0.20
paho-mqtt==2.1.0
